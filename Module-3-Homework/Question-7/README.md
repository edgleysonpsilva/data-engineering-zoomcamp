## Question 7: External Table Data Storage

**Answer:** GCP Bucket

An external table in BigQuery creates a schema definition inside BigQuery, but the actual data remains in the original source location. In this assignment, we defined the `uris` parameter pointing to `gs:...`, which refers to a Google Cloud Storage (GCP) Bucket.
