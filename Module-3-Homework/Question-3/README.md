## Question 3: 

### Explanation:
BigQuery stores data by columns, unlike row-oriented databases that read the entire row even if you only need one attribute. BigQuery allows you to access individual columns independently.
Adding more columns to a `SELECT` statement directly increases the volume of data scanned.